# 分类关键词分词匹配说明

## 概述

分类关键词匹配已从**单关键字匹配**升级为**分词匹配**，使匹配更智能、更易理解。

## 工作原理

### 1. 分词策略

对输入文本进行多维度分词：

1. **完整文本匹配**：保留原始完整文本（最高优先级）
2. **分隔符分词**：按常见分隔符（空格、标点等）分割
3. **滑动窗口分词**：提取 2-4 字词（中文常见词长度）

### 2. 匹配流程

```
输入文本："京东外卖 - 老手艺肉酱米线"
    ↓
分词结果：
  - "京东外卖 - 老手艺肉酱米线"（完整文本）
  - "京东外卖"（分隔符分割）
  - "老手艺肉酱米线"（分隔符分割）
  - "京东"、"外卖"、"老手"、"手艺"、"肉酱"、"米线"（2字词）
  - "京东外"、"东外卖"、"老手艺"、"手艺肉"、"肉酱米"、"酱米线"（3字词）
  - "京东外卖"、"老手艺肉"、"手艺肉酱"、"肉酱米线"（4字词）
    ↓
按长度降序排序（长词优先匹配）
    ↓
依次匹配关键词表
    ↓
返回最佳匹配的分类ID
```

### 3. 匹配优先级

1. **用户自定义关键词** > 系统关键词
2. **权重高的关键词** > 权重低的关键词
3. **长词匹配** > 短词匹配
4. **完整匹配** > 部分匹配

## 优势

### 1. 更智能的匹配

**之前**：需要为每个变体单独配置关键词
```sql
-- 需要配置多个关键词
INSERT INTO category_keyword_mapping (category_id, keyword) VALUES (1602, '外卖');
INSERT INTO category_keyword_mapping (category_id, keyword) VALUES (1602, '京东外卖');
INSERT INTO category_keyword_mapping (category_id, keyword) VALUES (1602, '美团外卖');
```

**现在**：只需配置核心关键词
```sql
-- 只需配置核心关键词
INSERT INTO category_keyword_mapping (category_id, keyword) VALUES (1602, '外卖');
-- "京东外卖"、"美团外卖"、"饿了么外卖" 等都能自动匹配到"外卖"
```

### 2. 更易维护

- **关键词表更简洁**：只需存储核心关键词，不需要存储所有变体
- **理解更直观**：关键词表直接反映分类的核心特征
- **维护成本更低**：新增分类时只需添加核心关键词

### 3. 匹配更准确

- **长词优先**：自动优先匹配更长的词，减少误匹配
- **权重控制**：通过权重字段控制匹配优先级
- **双向匹配**：支持 `keyword LIKE '%text%'` 和 `text LIKE '%keyword%'`

## 使用示例

### 示例 1：餐饮分类匹配

**输入文本**：`"京东外卖 - 老手艺肉酱米线"`

**分词结果**：
- `"京东外卖 - 老手艺肉酱米线"`（完整）
- `"京东外卖"`（片段）
- `"老手艺肉酱米线"`（片段）
- `"外卖"`（2字词）
- `"米线"`（2字词）
- ...

**关键词表**：
```sql
category_id=1602, keyword='外卖', weight=3
category_id=1601, keyword='米线', weight=2
```

**匹配结果**：`category_id=1602`（外卖，权重更高）

### 示例 2：交通分类匹配

**输入文本**：`"滴滴出行 - 快车"`

**分词结果**：
- `"滴滴出行 - 快车"`
- `"滴滴出行"`
- `"快车"`
- `"滴滴"`、`"出行"`、`"快车"`（2字词）
- ...

**关键词表**：
```sql
category_id=1703, keyword='网约车', weight=3
category_id=1703, keyword='滴滴', weight=2
category_id=1702, keyword='出租车', weight=3
```

**匹配结果**：`category_id=1703`（网约车，通过"滴滴"匹配）

## 关键词表优化建议

### 1. 核心关键词原则

只存储**核心关键词**，不需要存储所有变体：

```sql
-- ✅ 推荐：只存储核心关键词
INSERT INTO category_keyword_mapping (category_id, keyword, weight) 
VALUES (1602, '外卖', 3);

-- ❌ 不推荐：存储所有变体（冗余）
INSERT INTO category_keyword_mapping (category_id, keyword, weight) 
VALUES (1602, '外卖', 3),
       (1602, '京东外卖', 3),
       (1602, '美团外卖', 3),
       (1602, '饿了么外卖', 3);
```

### 2. 权重设置建议

- **权重 3**：核心关键词，如"外卖"、"网约车"、"医院"
- **权重 2**：重要关键词，如"米线"、"快车"、"挂号"
- **权重 1**：一般关键词，如"京东"、"滴滴"、"三甲"

### 3. 关键词长度建议

- **2-4 字词**：最佳匹配效果
- **1 字词**：容易误匹配，谨慎使用
- **5 字以上**：匹配率低，建议拆分

## 技术实现

### 1. 分词工具类

`TextSegmentUtil.segment(String text)` 提供分词功能：
- 按分隔符分割
- 滑动窗口提取 2-4 字词
- 自动过滤无效词（纯数字、纯符号）
- 按长度降序排序

### 2. 匹配服务

`CategoryKeywordMappingServiceImpl.findCategoryIdByKeyword()` 实现分词匹配：
- 对输入文本分词
- 依次匹配关键词表
- 按优先级返回最佳匹配

### 3. 数据库查询

`CategoryKeywordMappingRepository.findMatchingKeywords()` 提供高效查询：
- 支持双向匹配（`keyword LIKE '%text%' OR text LIKE '%keyword%'`）
- 按权重和长度排序
- 只查询启用的关键词

## 迁移指南

### 从单关键字匹配迁移到分词匹配

1. **无需修改数据库**：现有关键词表可直接使用
2. **优化关键词表**（可选）：
   - 删除冗余的关键词变体
   - 只保留核心关键词
   - 调整权重设置

3. **测试验证**：
   - 使用常见交易文本测试匹配效果
   - 根据实际效果调整关键词和权重

## 注意事项

1. **性能考虑**：分词匹配会查询多个分词结果，但通过优先级排序和早期返回，性能影响可控
2. **匹配准确性**：长词优先策略确保更准确的匹配，但需要合理设置关键词权重
3. **关键词维护**：建议定期审查关键词表，删除冗余关键词，优化核心关键词
